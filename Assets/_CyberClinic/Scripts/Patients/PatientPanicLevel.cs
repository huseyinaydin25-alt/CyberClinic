namespace CyberClinic.Patients
{
    /// <summary>Qualitative panic band derived from numeric panic (0–100) at runtime.</summary>
    public enum PatientPanicLevel
    {
        Calm = 0,
        Uneasy = 1,
        Agitated = 2,
        Panicked = 3
    }
}
